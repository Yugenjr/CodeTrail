[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "codetrail-shared"
version = "0.1.0"
description = "CodeTrail shared package"
requires-python = ">=3.11"
readme = "README.md"

[tool.setuptools]
package-dir = {"" = "src"}
