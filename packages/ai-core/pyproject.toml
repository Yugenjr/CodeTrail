[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "codetrail-ai-core"
version = "0.1.0"
description = "CodeTrail AI core"
requires-python = ">=3.11"
readme = "README.md"

[tool.setuptools]
package-dir = {"" = "src"}
