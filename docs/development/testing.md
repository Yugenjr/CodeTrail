# Testing

Run the project's tests with `pytest`.

```bash
pytest -q
```

Notes

- The repository includes simple CLI help tests under `tests/test_cli_commands.py` that exercise available commands.
- Add unit tests for behavior-critical logic (repository analysis, detection rules) to prevent regressions.
