# Project Structure

Top-level layout relevant to contributors:

- `apps/cli/` — CLI application and command implementations.
- `packages/` — reusable packages (skill-engine, issue-engine, shared libs).
- `tests/` — pytest tests for CLI and core utilities.
- `docs/` — this documentation set.

When adding features, prefer small, focused changes and include tests.
