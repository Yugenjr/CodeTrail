# Skill Engine

The Skill Engine aggregates signals across repositories to produce a portfolio-level view used by `codetrail profile --skills`.

Responsibilities:

- Language aggregation across repositories.
- Framework and tool scoring using tiered evidence.
- Caching and versioning of computed results to reduce API calls.
